[tox]
envlist = py310
skipsdist = True
 
[testenv]
deps =
    -r requirements.txt
    pytest
    coverage
    pytest-cov
commands = 
    ; x docker-compose -f docker-compose-dev.yml up -d
    pytest -s
    ;  docker-compose -f docker-compose-dev.yml down
setenv =
   PYTHONPATH =  .
 
[coverage:run]